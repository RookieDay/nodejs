/**
 * process
 */

// setTimeout(function () {
//   console.log('hello world');
// },3000);

// abort() 方法 可以立即终止 当前 node 进程
// 一般你想让程序退出的时候，不要使用这个 函数，太猛了
// process.abort();


// arch 可以获取操作系统的 位数 x64 x86
// console.log(process.arch);

console.log(process.argv);
